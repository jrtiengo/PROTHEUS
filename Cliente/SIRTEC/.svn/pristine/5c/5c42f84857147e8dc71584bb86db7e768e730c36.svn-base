#include 'totvs.ch'

User Function M160PLAN ()

	Local aArea	:= GetArea()
	Local aRet  := {}
	Local _i
	Local cAlSC8    := PARAMIXB[1]  
	Local aVal      := PARAMIXB[2]   
	Local aCam      := PARAMIXB[3]  

// For _i := 1 to Len(aVal)
// 	Aadd(aVal[1],11)
 //Next
 
 //Aadd(aCam,"PLN_STOTCOT")

 
 	aadd(aRet,cAlSC8)
	aadd(aRet,aVal)
	aadd(aRet,aCam)
	
	RestArea(aArea)
 
 Return aVal//aRet